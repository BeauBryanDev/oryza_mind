# OryzaMind backend dependencies.
#

#     python3 -m venv oryza && oryza/bin/pip install -r requirements.txt
#

# HTTP layer
fastapi>=0.116
uvicorn[standard]>=0.35
python-multipart>=0.0.20       
pydantic>=2.9
pydantic-settings>=2.5
python-dotenv>=1.0              # pydantic-settings env_file support

# Agent (Gemini via LangChain)

langchain>=0.3,<1.0
langchain-core>=0.3,<1.0
langchain-google-genai>=2.1,<3.0

# Retrieval
# >=4.22: earlier versions serialize a bool filter into protobuf's value_int
# (isinstance(False, int) is True), which protobuf 7 rejects. That breaks the
# is_inoculation_protocol filter, so it is a safety-relevant floor.
weaviate-client>=4.22
# e5-large-v2 must be reproduced exactly: OryzaMindChunk is vectorizer:none,
# so query vectors are built in-process. Pulls torch transitively.
# For CPU-only torch (~200MB instead of ~2GB with CUDA):
#     oryza/bin/pip install torch --index-url https://download.pytorch.org/whl/cpu
# ...before installing this file.
sentence-transformers>=3.0

# Vision
onnxruntime>=1.19
# Not 4.9.0.80: that wheel is built against numpy 1.x and fails to import on
# py3.13, where no numpy<2 wheel exists. 4.10.0.84 is the first numpy-2 build.
opencv-python-headless==4.10.0.84  # decode, letterbox, mask resize
numpy>=1.26
# Spike classifier resize only. PIL antialiases on downscale, cv2's
# INTER_LINEAR does not, and that difference flips the class on large photos.
pillow>=10.4

# Dev / test
pytest>=8.3
pytest-asyncio>=0.24
httpx>=0.27                      # fastapi.testclient.TestClient runs on httpx


#
# motor>=3.6
# pymongo>=4.9
#

